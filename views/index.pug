extends layout

block content
  h1= title

  ul(class='items')
    each item in contents
      li(class='item')
        div(class='content')
          a(href=item.link)
            h3= item.title
          p= item.description
